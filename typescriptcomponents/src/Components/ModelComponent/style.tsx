import { makeStaticStyles, makeStyles } from "@fluentui/react-components";

export const useStaticStyle = makeStaticStyles({
  ".r1svjbtt": {
    maxWidth: "950px",
  },
});
export const useStyles = makeStyles({
  ModelSurface: {
    borderRadius: "16px",
  },
  ModelHeading: {
    display: "flex",
    justifyContent: "space-between",
  },
});
