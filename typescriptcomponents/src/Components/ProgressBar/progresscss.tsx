import { makeStyles } from "@fluentui/react-components";

export const useStyles = makeStyles({
    root:{
        textAlign: "center",
        fontSize: "7px",
        fontStyle:"normal",
        fontWeight:700,
        height:"14px",
        width:"14px",
    }

});